package dev.posco.hiona.jobs

class FinnhubDBCandleTests extends munit.FunSuite {
  test("we should be able to instantiate any Lambda") {
    // this requires AWS credentials to be found
    // val lam = new FinnhubDBCandleLambda
    // assert(lam != null)
    assert(true)
  }
}
